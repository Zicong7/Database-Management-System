import { Role } from './role';

export class User {
  // tslint:disable-next-line:variable-name
  user_id: number;
  // tslint:disable-next-line:variable-name
  user_fname: string;
  // tslint:disable-next-line:variable-name
  user_lname: string;
  // tslint:disable-next-line:variable-name
  user_email: string;
  // tslint:disable-next-line:variable-name
  user_gender: string;
  // tslint:disable-next-line:variable-name
  user_dob: Date;
}
